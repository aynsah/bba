$('#search-output-table').append('<%= j render partial: "campaign", locales: {campaigns: @campaigns} %>');
<% if @campaigns.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@campaigns) %>');
<% else %>
  $('.pagination').remove();
<% end %>